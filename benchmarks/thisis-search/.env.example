# Search Endpoint Benchmark Configuration

# Base URL of your search API (required)
BASE_URL=https://localhost:8080

# Number of concurrent synthetic users
USERS=300

# Total test duration in seconds
DURATION_SEC=300

# Think time range (ms) - random delay between requests per user
MIN_THINK_MS=5000
MAX_THINK_MS=10000
